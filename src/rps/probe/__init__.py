"""Diagnostics that establish what a ``.drp`` file actually is.

Nothing in the searcher depends on this package. It exists so that version 2 —
the structural parser that reports project / timeline / track / timecode — can be
built from measured facts about real files instead of from assumptions about an
undocumented format.
"""
